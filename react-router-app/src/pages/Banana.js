import React from 'react';
import PropTypes from 'prop-types';

const Banana = props => {
    return (
        <div>
            this is banana page.
        </div>
    );
};

Banana.propTypes = {
    
};

export default Banana;
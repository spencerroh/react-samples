import React from 'react';
import PropTypes from 'prop-types';

const Carrot = props => {
    return (
        <div>
            this is Carrot page.
        </div>
    );
};

Carrot.propTypes = {
    
};

export default Carrot;
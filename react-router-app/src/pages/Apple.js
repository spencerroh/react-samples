import React from 'react';
import PropTypes from 'prop-types';

const Apple = props => {
    return (
        <div>
            This is apple page.
        </div>
    );
};

Apple.propTypes = {
    
};

export default Apple;